# 官方 Hermes Agent 版本
# v0.20.0 (Herald Release, 2026-08-03, tag v2026.8.3) 起官方停止 PyPI 分发（wheel/sdist 被构建守卫拦截），
# 仅支持源码安装（editable）或 Nix/Docker。本包改为随 FPK 内置完整源码（app/hermes-src）并在安装时
# 执行 `uv pip install -e "hermes-src[all]"`（editable 安装不触发 bdist_wheel 守卫）。
HERMES_VERSION=0.20.0
